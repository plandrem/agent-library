Install CCPM (Claude Code PM) into the current project.

Run the following command:

```bash
bash ~/ai/ccpm/install-project.sh "$PWD"
```

Report the output to the user. If there are any errors, help troubleshoot them.
