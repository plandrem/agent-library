# Git Commit

Check git status. Determine the relevant files to commit based on the most recent work. Only stage changes that you have directly modified in working on the current feature. Compose a succinct but descriptive summary of the changes for the commit message. NEVER include any indication of Claude or Anthropic the commit message. Then, execute the commit.